# Admin password (required for /admin login)
ADMIN_PASSWORD=change-me

# Vercel KV — required for persistent category/analytics storage on Vercel
# Create via: Vercel Dashboard → Storage → KV (or Upstash Redis integration)
KV_REST_API_URL=
KV_REST_API_TOKEN=
